version: '3.8'

services:
  nvim:
    platform: linux/arm64  # Use linux/amd64 on Intel machines
    build:
      context: .
      dockerfile: Dockerfile
      args:
        USERNAME: ${USER}
        USER_UID: 1000
        USER_GID: 1000
    image: nvim-dev:latest
    container_name: nvim-dev
    stdin_open: true
    tty: true
    volumes:
      # Mount your home directory
      - ${HOME}:${HOME}
    working_dir: ${HOME}
    environment:
      - HOME=${HOME}
      - USER=${USER}
      - TERM=${TERM:-xterm-256color}
      - COLORTERM=truecolor
    network_mode: host
